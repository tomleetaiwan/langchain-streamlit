# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: langchain-streamlit
services:
  web:
    project: ./src # path to your web project
    language: python # one of the supported languages
    host: containerapp # one of the supported Azure services
